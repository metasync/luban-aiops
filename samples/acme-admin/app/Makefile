# acme-admin sample-app routines (SPEC-059 R-4). Targets come from the shared
# fragments under ../../../mk/; run `make help`. Deliberately NOT listed in the
# root Makefile's IMAGE_PRODUCTS/PYTHON_PRODUCTS — `make build` is the platform's
# coordinated image path and must not grow a tutorial image. Build this one with
# `make deploy-sample-app` (or `make -C samples/acme-admin/app build`).

IMAGE_NAME := acme-admin

include ../../../mk/image.mk
include ../../../mk/python.mk
