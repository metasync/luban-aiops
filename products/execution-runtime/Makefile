# execution-runtime product routines.
#
# Usage:  make -C products/execution-runtime <target>   (or cd here and run make)
# Targets come from the shared fragments under ../../mk/. Run `make help`.

IMAGE_NAME := execution-runtime

include ../../mk/image.mk
include ../../mk/python.mk
