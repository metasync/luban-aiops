# agent-platform product routines.
#
# Usage:  make -C products/agent-platform <target>   (or cd here and run make)
# Targets come from the shared fragments under ../../mk/. Run `make help`.

IMAGE_NAME := agent-service

include ../../mk/image.mk
include ../../mk/python.mk
