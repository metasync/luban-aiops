# skills-hub product routines.
#
# Usage:  make -C products/skills-hub <target>   (or cd here and run make)
# Targets come from the shared fragments under ../../mk/. Run `make help`.

IMAGE_NAME := skills-hub

include ../../mk/image.mk
include ../../mk/python.mk
