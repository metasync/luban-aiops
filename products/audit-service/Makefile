# audit-service product routines.
#
# Usage:  make -C products/audit-service <target>   (or cd here and run make)
# Targets come from the shared fragments under ../../mk/. Run `make help`.

IMAGE_NAME := audit-service

include ../../mk/image.mk
include ../../mk/python.mk
