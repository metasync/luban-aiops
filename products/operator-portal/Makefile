# operator-portal product routines (static web UI served by nginx).
#
# Usage:  make -C products/operator-portal <target>   (or cd here and run make)
# Image targets only — this product has no Python test suite. Run `make help`.

IMAGE_NAME := web-ui

include ../../mk/image.mk
