# TEMPLATE — replace the placeholder with a real token before applying,
# e.g. `openssl rand -hex 32`. Never commit a real token. The same token
# lands in the platform secret as LUBAN_API_KEY (guide §4).
apiVersion: v1
kind: Secret
metadata:
  name: ollama-api-key
  labels:
    app: ollama
type: Opaque
stringData:
  OLLAMA_API_KEY: replace-with-ollama-api-key
