apiVersion: apps/v1
kind: Deployment
metadata:
  name: tool-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tool-gateway
  template:
    metadata:
      labels:
        app: tool-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: "8000"
    spec:
      # Service-link env vars collide with *_PORT/_HOST settings; service
      # discovery uses DNS names instead.
      enableServiceLinks: false
      serviceAccountName: tool-gateway
      containers:
        - name: tool-gateway
          image: luban-aiops/tool-gateway:dev-local
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
          envFrom:
            - configMapRef:
                name: platform-runtime-config
            - secretRef:
                name: tool-gateway-runtime-secrets
                optional: true
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: platform-policy
              mountPath: /etc/luban/policy
              readOnly: true
      volumes:
        - name: platform-policy
          configMap:
            name: platform-policy
