apiVersion: v1
kind: Service
metadata:
  name: browser-check-target
  labels:
    app: browser-check-target
spec:
  selector:
    app: browser-check-target
  ports:
    - name: http
      port: 8080
      targetPort: 8080
