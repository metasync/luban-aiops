Python services managed with `uv` (per-product `uv.lock`); FastAPI-style HTTP services; containerized via Dockerfiles built through a coordinated Makefile; Kubernetes deployment via Kustomize overlays; JSON Schema-based API contracts in `shared/shared-contracts/schemas`; YAML policy bundles validated against a shared schema.