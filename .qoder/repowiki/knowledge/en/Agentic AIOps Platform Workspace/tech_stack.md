Python services managed with `uv` (pinning interpreter via `.python-version` per product); container images built with Dockerfiles using a shared `shared/base-images/base-uv` base image; Kubernetes deployment via kustomize overlays under `shared/platform-ops/gitops`; end-to-end demos against a kind cluster; policy validation via JSON Schema scripts in `shared/shared-contracts/scripts`.