Python services built with FastAPI-style apps, managed via `uv` for dependency and environment control; container images based on a shared `base-uv` image; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops/dev-k8s`; API/event contracts defined as JSON Schema files in `shared/shared-contracts/schemas/`; policy enforcement driven by YAML policy bundles synchronized from a canonical source.