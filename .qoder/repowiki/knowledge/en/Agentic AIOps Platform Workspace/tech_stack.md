Python services managed with `uv` (lockfiles + `.python-version` pinning), FastAPI-style service layout (`app.py`/`main.py` entrypoints, `api/routes`, `services`, `schemas`, `core/config+telemetry+metrics`), Docker containerization per product, Kustomize GitOps overlays under `shared/platform-ops/gitops`, and JSON Schema validation for inter-service contracts in `shared/shared-contracts/schemas`.