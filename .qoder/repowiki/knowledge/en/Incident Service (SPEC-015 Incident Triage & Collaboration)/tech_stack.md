Python 3.11+ FastAPI + Uvicorn server; Pydantic v2 models bound to shared JSON schemas in `shared/shared-contracts/schemas/`; psycopg3 async driver for PostgreSQL; httpx for outbound calls to agent-platform and audit-service; PyJWT with RS256 for validating projected Kubernetes workload tokens against the cluster OIDC issuer JWKS; OpenTelemetry (OTLP HTTP exporter) and Prometheus client for observability; uv build system.