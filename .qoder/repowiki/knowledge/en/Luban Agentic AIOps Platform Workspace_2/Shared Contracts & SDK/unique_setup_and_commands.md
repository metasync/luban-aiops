Policy bundle changes must bump the `version` field in `policy-default.yaml`; correctness is enforced by running `python scripts/validate_policy.py` (defaulting to the canonical bundle), `make verify` (scenario matrix), and `make policy-diff CANDIDATE=<bundle>` to diff outcome transitions. Deployment pipelines also fingerprint the loaded bundle SHA-256 on readiness/matrix surfaces to confirm the exact file deployed.