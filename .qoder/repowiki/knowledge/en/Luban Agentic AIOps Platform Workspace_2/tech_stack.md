Python services managed with `uv` (per-product `.python-version` pins interpreter); FastAPI-based HTTP services packaged into Docker images built on a shared `base-uv` image; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; Playwright headless browser for tool-gateway's `web.*` tools; Elastic connector for observability/evidence; PostgreSQL and Redis backends for audit and session stores; Ant Design X for the operator portal frontend.