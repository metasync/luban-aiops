Python services managed with `uv` (lockfiles + `.python-version` pins); container images built with Docker using a shared `base-uv` image; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; policy evaluation driven by JSON-schema-validated YAML bundles in `shared/shared-contracts/policies/`; end-to-end demos scripted in shell under `shared/platform-ops/e2e/`.