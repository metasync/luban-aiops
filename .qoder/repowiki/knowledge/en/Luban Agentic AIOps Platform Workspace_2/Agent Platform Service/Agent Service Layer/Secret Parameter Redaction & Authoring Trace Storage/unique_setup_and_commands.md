The build includes a `make verify` leg running `validate_secret_vocabulary.py` that fails if the secret-parameter tuples in this module diverge from the gateway twin or if `CREDENTIAL_HOLE` drifts from the skills-hub copy. Postgres tables are created idempotently on first use, including an in-place `ALTER TABLE ... ADD COLUMN IF NOT EXISTS flow_origin` migration for pre-R-4 clusters.