Entry point is the `audit-service` console script defined in `pyproject.toml` (`audit_service.main:run`); local development uses `uv` with `.python-version` pinning. Docker image built via the provided `Dockerfile` and deployed from `shared/platform-ops/gitops/dev-k8s/base/audit-service/audit-service-deployment.yaml`, which expects `platform-runtime-config` ConfigMap and optional `audit-service-runtime-secrets` Secret mounted via `envFrom`.