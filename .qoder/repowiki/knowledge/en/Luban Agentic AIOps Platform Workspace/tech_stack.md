Python services managed with `uv` (lockfiles per product); FastAPI-style service layout (`app.py`/`main.py` entrypoints, `api/routes`, `services`, `schemas`, `core` config/telemetry); container images built from Dockerfiles using a shared `base-uv` image; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; policy evaluation driven by YAML policy bundles validated against JSON Schema in `shared/shared-contracts/policies`.