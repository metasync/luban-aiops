Python services managed with `uv` (pinning 3.12 via `.python-version`); container images built from a shared `base-uv` image based on Amazon Linux 2023; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; policy bundles validated as JSON Schema documents; end-to-end demos driven by shell scripts against a deployed cluster.