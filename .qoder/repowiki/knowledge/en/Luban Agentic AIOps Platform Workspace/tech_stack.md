Python FastAPI services managed with `uv` (per-product `pyproject.toml` + `uv.lock`); containerized via Dockerfiles built through shared fragments in `mk/`; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops/`; API/event contracts enforced as JSON Schema files in `shared/shared-contracts/schemas/`; policy bundles validated with custom Python scripts under `shared/shared-contracts/scripts/`.