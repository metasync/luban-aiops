Python services managed with `uv` (lockfiles + environment isolation); FastAPI-based HTTP APIs with Pydantic-style schemas; containerized via Docker using a shared `base-uv` image; Kubernetes deployment via Kustomize overlays; policy evaluation driven by YAML policy bundles validated against JSON Schema; observability via OpenTelemetry conventions defined in `shared/shared-contracts/observability-conventions.md`.