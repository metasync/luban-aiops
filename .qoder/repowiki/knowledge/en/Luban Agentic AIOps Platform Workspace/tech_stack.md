Python services standardize on `uv` for environment/package management (pinned via `.python-version`); container images are built from a shared Amazon Linux 2023 base image in `shared/base-images/base-uv`; Kubernetes deployment uses Kustomize overlays under `shared/platform-ops/gitops`; policy evaluation is driven by the canonical YAML bundle in `shared/shared-contracts/policies/policy-default.yaml` validated against JSON Schema.