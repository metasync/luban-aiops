Python backend services managed with `uv` (pinning interpreter via `.python-version` per product); container images built from Dockerfiles using a shared `base-uv` image; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; frontend SPA (`operator-portal/web-ui`) built with Vite and tested with Vitest; policy bundles validated against JSON schemas; PostgreSQL-backed durable stores for sessions, audit trail, and incidents.