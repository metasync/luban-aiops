Python services managed with `uv` (pinning interpreter via `.python-version`); container images built from Dockerfiles using a shared `base-uv` image; deployment via Kustomize overlays; policy enforcement via YAML policies validated against a JSON schema in `shared/shared-contracts`; observability emitted as structured events consumed by a dedicated `audit-service` backed by in-memory or PostgreSQL storage.