Python services managed with `uv` (interpreter pinned per product via `.python-version`); container images built from per-product `Dockerfile`s orchestrated by the root Makefile; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops/`; policy documents validated against JSON schemas in `shared/shared-contracts/`; end-to-end demos driven by shell scripts in `shared/platform-ops/e2e/` and `samples/acme-admin/`.