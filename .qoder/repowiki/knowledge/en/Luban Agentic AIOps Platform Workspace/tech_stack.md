Python 3.11+ with `uv` for dependency/environment management and `uv_build` as the PEP 517 backend; FastAPI + Uvicorn for all HTTP services; OpenTelemetry OTLP exporters for observability; PostgreSQL and Redis as persistence backends; Kustomize overlays under `shared/platform-ops/gitops` for Kubernetes deployment; JSON Schema files in `shared/shared-contracts/schemas` define the v1/v2 API contracts consumed across products.