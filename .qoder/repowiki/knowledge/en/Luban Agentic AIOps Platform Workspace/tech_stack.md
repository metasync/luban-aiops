Python services managed with `uv` (per-product `.python-version`, `uv.lock`, `pyproject.toml`); container images built via Dockerfiles orchestrated by a root Makefile; Kubernetes deployment via Kustomize overlays under `shared/platform-ops/gitops`; policy bundles validated against JSON schema with scripts in `shared/shared-contracts/scripts`; end-to-end demos driven by shell scripts under `shared/platform-ops/e2e`.