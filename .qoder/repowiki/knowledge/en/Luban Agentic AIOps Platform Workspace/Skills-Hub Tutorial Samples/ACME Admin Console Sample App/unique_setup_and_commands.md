Requires the platform to be deployed first, then `make deploy-sample-app` to build the image, apply `deploy/`, and run assertions; credentials come from running `shared/platform-ops/gitops/sync-browser-credentials.sh dev-luban-aiops` which writes the `acme-admin-credentials` secret and matching browser credential entry. Demos run via individual `*/demo/demo.sh` scripts or `RUN_CHAT_LEG=true samples/acme-admin/demo-suite.sh` after port-forwarding identity-service and platform-gateway.