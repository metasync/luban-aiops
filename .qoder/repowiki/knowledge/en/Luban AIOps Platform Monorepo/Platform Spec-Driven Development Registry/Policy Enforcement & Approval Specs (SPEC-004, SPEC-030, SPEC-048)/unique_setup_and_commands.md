`make sync-policy` copies the canonical bundle into every packaged and overlay location; `make verify` runs schema checks plus the scenario-expectation harness against the canonical bundle; `make policy-diff CANDIDATE=<path>` produces an impact report comparing a candidate bundle to the canonical one; bundles are loaded at startup from `GATEWAY_POLICY_PATH` and take effect only on pod restart (no hot reload).