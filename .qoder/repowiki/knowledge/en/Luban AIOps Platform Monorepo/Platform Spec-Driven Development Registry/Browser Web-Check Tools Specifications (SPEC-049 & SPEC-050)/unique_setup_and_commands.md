Opt-in via GATEWAY_BROWSER_ENABLED=true plus GATEWAY_BROWSER_ALLOW_ORIGINS allowlist and GATEWAY_BROWSER_CREDENTIAL_SETS pointing to a secret-mounted JSON file; smoke test runs via shared/platform-ops/e2e/browser-check-demo.sh; sample skills are installed out-of-band through make deploy-samples which packs selected samples/skill/*.md into a skills-samples ConfigMap mounted at /skills/samples.