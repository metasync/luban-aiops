`make verify` is the pre-commit/pre-push gate running tests, kustomize overlay renders, policy validation, and version lockstep checks; `make build` produces coordinated images tagged `<semver>-dev-k8s[-profile]-<gitsha>` and writes `shared/platform-ops/gitops/dev-k8s/.images.env`; `make e2e` runs skills/incident/mutating demo scripts against a deployed dev cluster after port-forwarding platform-gateway and identity-service.