Python 3.11+ with uv for environment/package management; FastAPI + Uvicorn for HTTP APIs; Pydantic v2 for request/response models; OpenTelemetry (OTLP HTTP exporter) plus Prometheus metrics for observability; PostgreSQL and Redis as optional backends (agent-platform); JWT/RSA-256 tokens verified against JWKS from identity-broker; Kubernetes/kustomize for GitOps deployments; Docker images built on a shared `base-uv` image.