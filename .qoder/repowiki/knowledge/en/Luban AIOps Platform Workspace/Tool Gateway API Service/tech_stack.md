Python ≥3.11 with FastAPI + Uvicorn as the HTTP server; PyJWT for local bearer-token verification; PyYAML for policy bundles; httpx for outbound calls to identity, skills, incidents, and audit services; elasticsearch and kubernetes client libraries for connector backends; OpenTelemetry SDK with OTLP HTTP exporter and FastAPI/httpx/logging instrumentation; Prometheus client for metrics.