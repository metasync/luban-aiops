Prometheus metrics via `prometheus-client` (RED metrics on every service); OpenTelemetry SDK with FastAPI auto-instrumentation and OTLP exporter targeting Elastic APM; PostgreSQL-backed `AuditStore` with WAL durability for the audit trail; Elasticsearch Python client for the read-only Elastic connector; JSON Schema contracts in `shared/shared-contracts/schemas/` binding emitters and consumers.