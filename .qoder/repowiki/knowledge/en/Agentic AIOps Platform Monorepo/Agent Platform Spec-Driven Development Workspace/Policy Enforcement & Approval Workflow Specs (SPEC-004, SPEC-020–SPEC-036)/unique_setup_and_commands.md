Specs reference the project's build/release surface: `make sync-policy` propagates the default policy bundle to every product and overlay, `make validate-policy` checks bundle validity, `make verify` runs the full product test suites across Python products and overlays, and dev Kustomize overlays mount the policy via ConfigMap and set `GATEWAY_POLICY_PATH`; e2e validation uses `shared/platform-ops/e2e/mutating-demo.sh`.