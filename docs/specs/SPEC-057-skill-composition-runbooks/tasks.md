# SPEC-057 Tasks: Skill Composition — Validated Runbooks of Single-Target Skills

Task states: `[ ]` pending, `[x]` done. Keep tasks small and tied to requirement IDs.
Ordering follows `plan.md`'s Sequencing And Dependencies — the contract + mirrors
(R-1/R-3) come first and everything validates against them; the skills-hub config
knob + structural validation (R-2 structural) depend on the contract; the resolution
pass (R-2 resolution + the derived `risk_class`) depends on the structural layer; the
read-path enrichment (R-6) depends on a stored, resolved composition; the
agent-platform purity + rebind tests (R-4/R-5) depend only on the `sub_skills`
vocabulary and run in parallel; the portal viewer + badge (R-7) depend on the read
path; the samples demo (R-8) depends on the resolution pass + read path; docs/release
depend on all. Tests land with their requirement — the per-requirement `tests:`
bullets are the asserting tests the Delivery Gate maps R-1…R-8 onto (ADR-0008).

> **This is a design pass.** No product code, contract, or config changes yet: the
> operator reviews `plan.md` + `tasks.md` before implementation begins, per the
> spec-driven workflow. Every box is `[ ]` pending. Two `Impact` refinements and
> three interpretations are recorded in `plan.md`'s **Resolved At Plan Time** for
> explicit operator sign-off before stage 1 starts.

## R-1: A `composition` skill class, additive on the existing contract

- [x] `shared/shared-contracts/schemas/skill.schema.json`: retitle `"Skill (v2)"` → `"Skill (v3)"`; extend the `kind` enum `["knowledge","executable_flow"]` → `["knowledge","executable_flow","composition"]`; add an **optional** top-level `sub_skills` array whose items are `{ skill_id (required, same pattern as the top-level `skill_id`), note (optional string ≤ 200) }` with `additionalProperties: false`; extend the description to name the composition class + Skill v3 additivity (R-1)
- [x] `products/skills-hub/src/skills_hub/schemas/skill.py`: add a `SubSkillRef` model (`extra="forbid"`; `skill_id: str`, `note: str | None = Field(default=None, max_length=200)`); add `sub_skills: list[SubSkillRef] | None = None`; widen the `kind` pattern to `^(knowledge|executable_flow|composition)$` (R-1)
- [x] `products/skills-hub/src/skills_hub/services/ingestion.py`: `ALLOWED_KEYS += {"sub_skills"}`; `VALID_KINDS += ("composition",)` (a `COMPOSITION_KIND` constant beside `EXECUTABLE_FLOW_KIND`) (R-1)
- [x] `products/skills-hub/src/skills_hub/services/skill_store.py`: add a `sub_skills JSONB` column on **both** backends — the `_DDL` `CREATE TABLE` + an idempotent `ALTER TABLE skills ADD COLUMN IF NOT EXISTS sub_skills JSONB` (the `steps` precedent at `:190`), the `_INSERT` column/value + `ON CONFLICT DO UPDATE`, `_ROW_COLUMNS`, the `_row_to_skill` `isinstance(..., list)` read guard, and the `Jsonb(sub_skills) if sub_skills is not None else None` insert wrapper in `replace_source` (R-1 / plan invariant 3)
- [x] derived `risk_class` **reuses the existing top-level field** (no new field or column): computed in R-2's resolution pass and persisted so `summary()` (`schemas/skill.py:63-65`) carries it to the list badge; a composition author-declares no `risk_class` (rejected in R-2) (R-1)
- [x] `shared/shared-contracts/skill-format.md`: bump v2 → v3; add a `sub_skills` row + a `kind: composition` value to the frontmatter-keys table; add a **"Composition skills (v3)"** section (ordered reference list, no authority, derived display-only `risk_class`, additive on a v2 consumer) (R-1)
- [x] `products/operator-portal/web-ui/app/src/chat/SkillContentViewer.tsx`: add `sub_skills` to the `SkillDetail` interface; `views/control/SkillsView.tsx`: add `risk_class` to the `SkillRecord` interface (the read side wired in R-7) (R-1 / R-7)
- [x] tests (`test_contracts.py`): a v3 composition validates against `skill.schema.json`; the property-set equality (`test_model_properties_match_contract_properties`) now includes `sub_skills`; the `kind` enum-parity includes `composition`; the guard **fires** when a mirror drops `sub_skills` or `composition` (prove sensitivity, not vacuity) (R-1)
- [x] tests (`test_skill_store.py`): `sub_skills` round-trips on **both** backends (in-memory + Postgres `Jsonb` write / `isinstance(list)` read); a NULL `sub_skills` reads back omitted (R-1)
- [x] tests (`test_ingestion.py`): a minimal valid composition ingests; a knowledge skill and an executable_flow skill validate exactly as today (additivity — no existing skill breaks) (R-1)

## R-2: Ingestion validation fails closed

- [x] `products/skills-hub/src/skills_hub/core/config.py`: add `composition_max_sub_skills: int = 8` to `SkillsSettings`; read `SKILLS_COMPOSITION_MAX_SUB_SKILLS` in `from_env` with a `SettingsError` guard for a value < 1 (fail-fast, consistent with `parse_sources`) (R-2 / OQ-4)
- [x] `shared/platform-ops/gitops/dev-k8s/base/skills-hub/runtime-config.env`: add `SKILLS_COMPOSITION_MAX_SUB_SKILLS=8` beside `SKILLS_SYNC_INTERVAL_SECONDS` (R-2)
- [x] `docs/guides/configuration-reference.md`: add a Feature Activation Matrix row for `SKILLS_COMPOSITION_MAX_SUB_SKILLS` (default `8`, origin runtime-config) beside the other `SKILLS_*` knobs (R-2)
- [x] `products/skills-hub/src/skills_hub/services/ingestion.py`: add a pure `_validate_composition(source_id, rel_path, frontmatter, max_sub_skills)`, called from `_validate_frontmatter` beside `_validate_steps` (`:268-270`); add a `MAX_SUB_SKILLS = 8` module default and thread `max_sub_skills` through `_validate_frontmatter` / `ingest_directory` / `validate_document` (R-2 structural)
- [x] `_validate_composition` **structural** rejections: `sub_skills` must be a non-empty list; each item a mapping with a required `skill_id` (id pattern) + an optional `note ≤ 200`; **no unknown item key**; **no duplicate** `skill_id`; count ≤ `max_sub_skills`; `kind: composition` **requires** `sub_skills`; a composition declares **no** `web_target`, **no** `steps`, **no** `risk_class` (R-2 / R-3)
- [x] `products/skills-hub/src/skills_hub/services/sync.py`: add `_resolve_compositions` in `sync_once` **between** `ingest_directory` and `replace_source` (`:201-204`), where `self._store` + `self._settings` are reachable; build the resolution index as *this source's fresh records overlaid on `store.get()` for cross-source ids* (R-2 resolution / plan Resolved At Plan Time 4)
- [x] `_resolve_compositions` **resolution** rejections (drop the record + append a `Rejection`): a sub-skill that is **unresolved**; a sub-skill that is itself a **`composition`** (no nesting). Single-target is **not** an active rejection — it is structurally guaranteed by the scalar `web_target` field (a resolved `Skill` declares exactly one target or none, never several), so there is nothing to check at resolution time; the two facts that *can* fail (unresolved, nested) are the active rejections. The same pass **derives + persists** the composition's display `risk_class` via `model_copy(update=...)` (`write` if any resolved sub-skill is `write`, else `read`) (R-2 / R-1)
- [x] `sync_once` passes `self._settings.composition_max_sub_skills` into `ingest_directory`; the `validate_document` draft path (`api/routes/skills.py:178`) and the `python -m skills_hub.validate` CLI (`validate.py:37-39`) resolve the configured cap so the pre-flight matches sync (R-2)
- [x] `products/skills-hub/src/skills_hub/services/sync.py`: add a `composition` bucket to `_rejection_category` for the resolution-layer rejections (unresolved / nested), keeping the cardinality guard; structural rejections stay in `frontmatter` (R-2)
- [x] confirm **no new audit event type**: a composition rejection rides the existing `skills_synced` rejected count + per-source status (`sync.py:216-234`) (R-2)
- [x] tests (`test_config.py`): `SKILLS_COMPOSITION_MAX_SUB_SKILLS` parses; a value < 1 raises `SettingsError`; absent defaults to 8 (R-2)
- [x] tests (`test_ingestion.py`, structural): a duplicate `skill_id` is rejected; over-cap is rejected; `kind: composition` without `sub_skills` is rejected; a composition declaring its own `web_target` / `steps` / `risk_class` is rejected; a `note > 200` is rejected; `validate_document` + the `validate` CLI reject the same documents (the shared structural layer) (R-2)
- [x] tests (`test_sync.py`, resolution): an unresolved sub-skill is dropped + a `Rejection` recorded; a sub-skill that is itself a `composition` is rejected; a cross-source sub-skill is rejected on the cycle before its source syncs and **accepted after** (eventual consistency); the derived `risk_class` is `write` when any sub-skill is `write` else `read`, and is persisted; a rejection increments the `skills_synced` rejected count with **no new event type** (R-2)

## R-3: No control flow

- [x] `shared/shared-contracts/schemas/skill.schema.json`: the `sub_skills` item keeps `additionalProperties: false`, and its description records the rationale (an interpreter would need loops, and a loop defeats `GATEWAY_BROWSER_FLOW_MAX_STEPS`, the only bound on an unlocked browser flow) (R-3)
- [x] `shared/shared-contracts/skill-format.md`: the v3 section states there is **no** branch / loop / conditional / retry / early-exit vocabulary, and a `note` is a string, never interpreted (R-3)
- [x] tests (`test_ingestion.py`): a composition carrying any unrecognized sequencing key (`if` / `loop` / `retry` / `on_fail`) on a `sub_skills` item is **rejected**, pinning that `additionalProperties: false` stays true (R-3)
- [x] tests (`test_contracts.py`): the schema rejects a `sub_skills` item carrying an extra key (the JSON-schema half of the same invariant) (R-3)

## R-4: A composition carries no authority; each sub-skill keeps its own gate

- [x] **no src change** in `flow_approvals.py` / `runtime_kernel.py` / `execution_signing.py` / the tool-gateway deviation guard / the policy engine (R-4)
- [x] tests (agent-platform `test_skill_composition_purity.py`, new): a grep/AST purity assertion that `sub_skills` never reaches the trust path — absent from `runtime_kernel.py`, `flow_approvals.py`, `execution_signing.py`, and **not** a field on `FlowContext` / `FlowApproval` (R-4)
- [x] tests (agent-platform `test_flow_approvals.py`): a **rebind re-park** test extending `test_record_overwrites_previous_identity` (`:120`) — recording a second sub-skill's `(skill_id, origin)` over the first's armed approval leaves the identity non-matching, so the next write re-parks (R-4)
- [x] tests (tool-gateway): a grep assertion that the deviation guard (`tools/browser_connector.py`) never reads `sub_skills`; the bound-flow behavior is byte-for-byte unchanged (R-4)
- [x] tests (agent-platform): a mixed browser+infra composition's gate count is the **sum** of its browser bindings and its infra writes — asserted at the unit level (one `flow` card per distinct browser binding, one `action` card per infra write) and given a live leg by R-8 (R-4)

## R-5: Not a transaction — report-and-stop, re-entry from a named step

- [x] `shared/shared-contracts/skill-format.md` + `docs/guides/` operator docs: document the **report-and-stop** convention (on a sub-skill failure, report which sub-skill failed and stop; no rollback/compensation) and the re-entry window — the completed prefix derives from `execution_records` signed receipts, swept at **30 days** (`execution_records.py:31`), so outside it the operator restarts from the beginning (R-5)
- [x] the authored composition `body` (R-8's sample) carries the "on failure, report which sub-skill failed and stop" instruction as grounded guidance (R-5 / R-6)
- [x] **no new state store**: no composite-progress store, no persisted runbook-progress record (R-5)
- [x] tests (agent-platform): a purity assertion that **no new state store** is introduced for composition progress (grep for a composition-progress store/table → absent) (R-5)

## R-6: Delivery to the agent is grounded guidance, never platform sequencing

- [x] `products/skills-hub/src/skills_hub/api/routes/skills.py`: in `get_skill` (`:184-214`), project each `sub_skills` item to a display view `{ skill_id, note?, resolved_title?, resolved_web_target? }` by looking each sub-skill up in the store — the `search_skills` `score`/`excerpt` projection precedent (`:134-146`); read-path only, nothing resolved is persisted, the authored/stored item + schema keep just `skill_id` + `note` (R-6 / plan Resolved At Plan Time 1)
- [x] confirm **both gateways unchanged** (pure passthrough): tool-gateway `GetSkillTool` returns `response.json()` verbatim, so the enriched view reaches the model through the existing SPEC-014 grounded-guidance path; platform-gateway's `{skill_id:path}` proxy forwards it (R-6)
- [x] confirm the platform **never** pre-binds a sub-skill, never issues `web.navigate` on the model's behalf, and never enforces the declared order (R-6)
- [x] tests (`test_routes.py`): `get_skill` on a composition projects each item's `resolved_title` + `resolved_web_target`; the authored item stays `skill_id` + `note`; a non-composition `get_skill` is byte-identical to today (R-6)

## R-7: Authorization posture and the Studio authoring surface

- [x] **no new policy action, no new audit event type**: a composition reads under the existing `skills:read` and is authored in Studio under the existing `session:skill_graduate` posture (SPEC-056); no `policy-default.yaml` change, no bundle content-hash bump (R-7)
- [x] `products/operator-portal/web-ui/app/src/chat/SkillContentViewer.tsx`: render `sub_skills` as an **ordered list** (sub-skill title, declared target, note) in the Rendered view, reusing the SPEC-052 rendered/raw `Segmented` pattern; the Raw view shows the frontmatter verbatim (R-7)
- [x] `products/operator-portal/web-ui/app/src/views/control/SkillsView.tsx`: render the derived `risk_class` badge column from the `SkillRecord` (it already flows from `summary()`) (R-7)
- [x] `docs/agentic-aiops-platform/authorization-matrix.md`: **verify only** — compositions read under `skills:read`, authored under `session:skill_graduate`; no new row (R-7)
- [x] `docs/guides/portal-user-guide.md`: document that a composition is authored in Studio / by hand and merged to Git (the authoring posture), and that the Skills viewer renders its sub-skill list (R-7 / plan Resolved At Plan Time 2)
- [x] tests (`SkillContentViewer.test.tsx`): the `sub_skills` ordered list renders (title, target, note) in the Rendered view; the Raw view shows the frontmatter (R-7)
- [x] tests (`SkillsView.test.tsx`): the derived `risk_class` badge renders for a composition row (R-7)
- [x] tests: `make policy-diff` reports **zero** outcome transitions across all (role, action) pairs — the assertion the "no new policy vocabulary" decision leaves behind (R-7)

## R-8: Delivery traceability per ADR-0008

- [x] `samples/acme-admin/composition/skill/<Name>.md`: a `kind: composition` document whose `sub_skills` reference the two existing published single-target skills `samples/password-reset-resetacmepassword` (browser write flow) + `samples/lock-unlock-user-lockunlockuser` (infra `http.post` write), in declared order, each with a `note`; the `body` carries the report-and-stop guidance (R-5/R-6/R-8 / plan Resolved At Plan Time 5)
- [x] `samples/acme-admin/composition/README.md` + `WALKTHROUGH.md`: the house sample docs (what the composition shows, how to run the demo) (R-8)
- [x] `samples/acme-admin/composition/demo/demo.sh`: **deterministic legs** assert ingestion/validation (the composition resolves; a nested reference is rejected; the cap rejects; the derived `risk_class` is `write` because password-reset is `write`; the sub-skill ids are the two mounted ids) and an **opt-in live chat leg** (`RUN_CHAT_LEG=true`) asserts the multi-binding gate count (**2** cards > any single sub-skill's **1**) and that no card claims authority over a sub-skill it does not name (R-8)
- [x] `samples/acme-admin/demo-suite.sh`: add `composition` to `DEMOS` and `samples/composition-<name>` to the preflight `expected` id set (the mounted set grows from five to six distinct ids) (R-8)
- [x] confirm `deploy-samples.sh`'s `find -type d -name skill` discovery (`:97`) mounts the composition (it ships a `skill/` dir) as `samples/composition-<name>`, in the **same** `samples` source as its sub-skills so resolution is deterministic within one cycle (R-8 / plan Resolved At Plan Time 4)
- [x] confirm the composition demo is exercised in the `make e2e` path via `demo-suite.sh` (already in the root `Makefile` e2e loop at `:215`) (R-8)
- [x] record in this `tasks.md`, at delivery, the ≥1 asserting test per R-1..R-7 acceptance criterion (the mapping the Delivery Gate checks) (R-8)

## Delivery Gate

- [x] all acceptance criteria in `spec.md` verified (R-1…R-8), each mapped to an asserting test above (ADR-0008):
  - R-1 → `test_contracts.py` (v3 validates, property-set + enum parity, guard fires) + `test_skill_store.py` (both-backend round-trip) + `test_ingestion.py` (additivity)
  - R-2 → `test_config.py` (knob) + `test_ingestion.py` (structural rejects) + `test_sync.py` (resolution: unresolved / nested / cross-source eventual consistency / derived `risk_class` / no new event type)
  - R-3 → `test_ingestion.py` (sequencing key rejected) + `test_contracts.py` (schema rejects an extra item key)
  - R-4 → agent-platform purity test + rebind re-park test + tool-gateway grep + the mixed browser+infra gate-count unit assertion (+ the R-8 live leg)
  - R-5 → the no-new-store purity assertion + the operator-doc re-entry window + the sample `body` report-and-stop guidance
  - R-6 → `test_routes.py` (get_skill projection; authored item unchanged; non-composition byte-identical)
  - R-7 → `SkillContentViewer.test.tsx` + `SkillsView.test.tsx` + `make policy-diff` zero transitions
  - R-8 → this mapping + the `samples/acme-admin/composition/demo.sh` exercised in the `make e2e` path
- [x] `make verify` green (all product pytest; 4 kustomize overlays; policy rules + scenarios **unchanged**; api + tools scenarios; version lockstep; the secret-vocabulary leg **unchanged** — no new secret literal) — record the `VERIFY_EXIT=0` count and `OK: all product and portal versions match VERSION=0.40.0`
  - recorded: `VERIFY_EXIT=0`, **2738** product tests (agent-platform 1327, audit-service 138, execution-runtime 73, identity-broker 60, incident-service 137, platform-gateway 376, skills-hub 229, tool-gateway 398); 4 kustomize overlays (`dev-k8s`, `runtime-profiles/{default,mutating-dev,browser-dev}`); `OK: 18 rule(s) validated against policy-rule.schema.json`; `OK: 137 scenario(s) passed for engine api; all granted pairs covered (90)`; `OK: 19 scenario(s) passed for engine tools; all granted pairs covered (13)`; `OK: all product and portal versions match VERSION=0.40.0`; all three secret-vocabulary legs unchanged (20 substrings, the `<credential-reference>` marker, 4 shape patterns)
  - **re-run on the final delivered tree** (after the Status/changelog flips, the release-note + `tasks.md` evidence, and the `demo.sh` leg-[3/5] status-parse fix — docs plus one sample shell script, none of which `make verify` executes; the demo runs under `make e2e`): `VERIFY_EXIT=0`, the same **2738**, every `OK:` line identical (18 rules; 137 api / 90 pairs; 19 tools / 13 pairs; `match VERSION=0.40.0`; all three secret-vocabulary legs) — so the delivered state, not just the pre-flip state, is the one that is green
- [x] `make policy-diff` reports **zero** outcome transitions (no new action; the bundle content-hash is unchanged) (R-7)
  - recorded: `CANDIDATE=shared/platform-ops/gitops/dev-k8s/base/shared/policy.yaml` (the deployed bundle); both engines report `pairs evaluated: 138 (6 roles x 23 actions)`, `no outcome transitions`, `unchanged pairs: 138`; canonical and candidate hash identically (`sha256 ee1f6c5f11f73892e10738ff2d1c0678a76ab463af6e8b9541c51bd89476102b`) and `git status` shows `shared/shared-contracts/policies/` + all three `POLICY_TARGETS` copies untouched. The `require-approval-tools-mutate` load WARNING is the documented SPEC-030 R-2 tools-engine skip, not a transition
- [x] portal `npm test` and `npm run build` green (`tsc --noEmit` + vite) — record the test/file count and `BUILD_EXIT=0`
  - recorded (re-run on the final tree): `Test Files 32 passed (32)`, `Tests 408 passed (408)`, `TEST_EXIT=0`; `BUILD_EXIT=0` (vite `✓ built in 865ms`, 5946 modules)
- [x] the `samples/acme-admin/composition/demo.sh` deterministic legs are green, and the `RUN_CHAT_LEG=true` live leg asserts the 2-card multi-binding gate count (R-8)
  - recorded (live on dev-k8s against the deployed v0.40.0 `skills-hub`; `E2E_EXIT=0`, `E2E_OK: all demos passed`): the `composition` rung passed **all five** deterministic legs — [1/5] mounted + declares no `web_target`/`steps`/`risk_class`; [2/5] `get_skill` resolves it to two ordered sub-skills (`samples/password-reset-resetacmepassword` target `http://acme-admin:8080/admin/`, then `samples/lock-unlock-user-lockunlockuser` infra/no target) at a **derived** `risk_class=write`; [3/5] both sub-skills are leaves (`kind='knowledge'`) and the `samples` source synced clean (`accepted=6`, no rejection named `RecoverAcmeAccount.md`); [4/5] the catalog-blind `/skills/validate` pre-flight rejects an over-cap list (`more than 8 sub_skills`), an authored `risk_class` (`a composition declares no risk_class (kind: composition)`) and a smuggled `on_fail` key (`sub_skill 1: unknown sub_skill keys: on_fail`, R-3); [5/5] a nested reference **passes** structure (`valid=true`) — the two-layer R-2 split. The suite header confirms `6 sample skill ids, pairwise distinct`. A first-pass demo bug (the leg-[3/5] status parse iterated the status *object* not its `sources` list) was found by this live gate and fixed; the platform behaviour was correct throughout.
  - **chat leg honestly not exercised in this cycle**: `make e2e` never sets `RUN_CHAT_LEG`, so the suite reported `chat legs: <off>` and the `[CHAT]` leg skipped — the live 2-card (flow + action) parking is opt-in and model-dependent. The 2-card multi-binding gate count is asserted **deterministically at the unit level** (R-4's mixed browser+infra gate-count test + the rebind re-park extension of `test_record_overwrites_previous_identity`), green in `make verify`; the live chat leg stays available via `RUN_CHAT_LEG=true` (the SPEC-056 act-4 model-dependence precedent).
- [x] `make build` produces the clean image (no `-dirty` tag); `make deploy` rolls it onto dev-k8s and all deployments reach `1/1 READY`; `make deploy-samples` re-installs + re-ingests the composition sample (R-8)
  - recorded (build + deploy + samples legs GREEN live, on the operator's "run the live e2e now" go-ahead): `make build` (`BUILD_EXIT=0`, 3m27s) produced all **9** images under one coordinated tag and wrote `shared/platform-ops/gitops/dev-k8s/.images.env`. `make deploy` (`DEPLOY_EXIT=0`) rolled them onto dev-k8s (context `orbstack`, ns `dev-luban-aiops`) and every luban deployment reached `1/1 READY` with `RESTARTS=0` — `agent-service`, `audit-service`, `execution-runtime`, `identity-service`, `incident-service`, `platform-gateway`, `skills-hub`, `tool-gateway` (2/2 with the browser sidecar), `web-ui` — re-syncing the 8 secret bundles and reconciling the Keycloak realm (`luban-aiops`, 6 test users + groups client scope) and the portal OIDC client; the live `skills-hub` reports `SKILLS_COMPOSITION_MAX_SUB_SKILLS=8`. `make deploy-sample-app` (`SAMPLEAPP_EXIT=0`) rebuilt `acme-admin` onto the same tag with every honesty assertion green (healthz 8 keys, 401→200 auth, four seeded users, credential set present, browser origin allowlisted, HTTP enabled). `make deploy-samples` (`SAMPLES_EXIT=0`) packed **6** sample skills including `composition-RecoverAcmeAccount.md`; `skills-hub` re-ingested and the composition **resolved in the live Postgres `skills` store** — a direct `SELECT` returns `samples/composition-recoveracmeaccount | kind=composition | risk_class=write | sub_skills` populated with both ordered sub-skill refs (R-1 both backends, R-2 resolution + derived `risk_class`, R-6 stored sub-skills).
  - **the clean-image half is deferred to post-commit** (the SPEC-056 line-139 precedent): this cycle ran on a dirty tree, so the coordinated tag was `0.40.0-dev-k8s-4c3d8ea-dirty-20260920230614` (`IMAGE_TAG` derives `-dirty-<timestamp>` from a non-empty `git status --porcelain`, and the deployed images are `amd64/linux` so the build ran under emulation on this arm64 host). A clean `0.40.0-dev-k8s-<sha>` tag is reachable only after the delivery commit, which the operator has not yet authorized; after it, `make build` is re-run to confirm the clean tag and the result appended here, exactly as SPEC-056 recorded it.
- [x] living state docs updated (see spec `Impact`): root `CHANGELOG.md` (a `## 0.40.0` section), a dated `docs/agentic-aiops-platform/release-notes/` note + its README index bullet, `docs/guides/configuration-reference.md` (the new knob — done in R-2), `docs/guides/portal-user-guide.md` (the Skills-viewer composition list + authoring posture — done in R-7), the skills guide / `skill-format.md` (v3 — done in R-1), affected RepoWiki pages (committed separately per convention)
  - recorded: `CHANGELOG.md` `## 0.40.0 — 2026-09-20` (Added + Changed) inserted above `## 0.39.1`; release note `2026-09-20-spec-057-skill-composition-runbooks.md` + its README index bullet at the top of `## Available Notes`; `configuration-reference.md` knob row (R-2), `portal-user-guide.md` Skills-catalog + Studio authoring-posture bullets (R-7), `skills-guide.md` + `skill-format.md` v3 (R-1) all in place. **RepoWiki pages: skipped** — `.qoder/repowiki/` is an IDE-regenerated cache whose hand-edits revert on regeneration (the v0.36.3 known limitation), recorded here rather than left silently unchecked
- [x] `docs/specs/README.md` SPEC-057 row → `delivered`
  - recorded: row 174's trailing status cell flipped `approved` → `delivered`; the row description is unchanged (frozen once approved)
- [x] `docs/adr/README.md` — **no new ADR**; ADR-0011 stays `accepted` (this is its Phase 1 realization), ADR-0007/0009/0010 stay `accepted` and unedited, ADR-0008 gate satisfied by this Delivery Gate (verify, do not edit)
  - verified: `git status docs/adr/` is empty — nothing edited, no new ADR file; the README lists `ADR-0007`, `ADR-0008`, `ADR-0011` all `accepted`
- [x] `docs/agentic-aiops-platform/delivery-roadmap.md` SPEC-057 / row 346 entry → `delivered` with the shipping version (v0.40.0) and the shipping evidence
  - recorded: row 346 cell 2 gained a `**Status delivered** (2026-09-20, v0.40.0) — …` sentence carrying the shipping evidence (verify 2738, portal 408/32, policy-diff zero across 138 pairs, the 2-card mixed browser+infra demo), and cell 3 now reads `own spec (SPEC-057), nineteenth R5 slice, Phase 1 — **delivered** 2026-09-20 (v0.40.0; retargeted v0.38.0 → v0.40.0 on 2026-09-17 …)`
- [x] `spec.md` Status block → `delivered`, release slice fixed to the shipping train (nineteenth R5 slice, v0.40.0), delivery changelog entry appended; requirement text R-1..R-8 + Non-Goals **unchanged** (frozen at approval)
  - recorded: `status: delivered`, a new `delivered: 2026-09-20 (v0.40.0)` line, `release slice: … (nineteenth R5 slice, v0.40.0 …)`, and a dated `2026-09-20: **delivered** …` changelog entry mapping all eight requirements to their asserting tests; R-1..R-8 + Non-Goals untouched
- [x] version lockstep bump **0.39.1 → 0.40.0** (MINOR): `VERSION` + every per-product lockstep constant (`pyproject.toml`, `metadata.py`, the `__init__.py` literals) + the `uv.lock` re-locks; `make validate-version` reports OK across every product and the portal
  - recorded: `VERSION` + 8 `pyproject.toml` + 8 `metadata.py` + 2 `__init__.py` at `0.40.0`, all 8 `uv.lock` re-locked via `uv lock` (`v0.39.1 -> v0.40.0`); `make validate-version` → `OK: all product and portal versions match VERSION=0.40.0`
