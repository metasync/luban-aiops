feat: SPEC-013 durable audit trail and operator portal shell redesign

Delivers SPEC-013 (durable audit trail) together with the operator
portal navigation redesign that ships alongside it.

SPEC-013:
- canonical audit-event schema + audit:read policy rule (auditor and
  platform-admin only, deny-by-default)
- new audit-service product: in-memory + Postgres stores, keyset
  pagination, batched retention eviction, health/metrics
- authenticated non-blocking ingest; fire-and-forget emitters in
  tool-gateway, platform-gateway, identity-broker (feature-gated)
- audit:read-gated query API proxied via platform-gateway with 4xx
  pass-through; read-only audit view in the portal
- dev-k8s Postgres + audit-service manifests and audit secret sync

Operator portal:
- two-column shell: sidebar (logo, function list) + one view at a time;
  <=800px hamburger off-canvas drawer with toggle-safe stacking
- sidebar footer: user card (initials avatar, icon-only sign in/out,
  popup menu with granted roles) and platform version card
- polish: sticky audit table headers, focus-visible rings,
  prefers-reduced-motion guards

Docs: release notes (incl. redesign change set), CHANGELOG, guides,
and READMEs updated; make verify green (456 tests, overlays, policy).
